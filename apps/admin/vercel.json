{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.7.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.7.0 --activate && pnpm build",
  "devCommand": "corepack enable && corepack prepare pnpm@9.7.0 --activate && pnpm dev"
}
