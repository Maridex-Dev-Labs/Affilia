version: '3.9'
services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
  backend:
    build: ./apps/backend
    env_file:
      - ./apps/backend/.env.example
    ports:
      - '8000:8000'
    depends_on:
      - redis
